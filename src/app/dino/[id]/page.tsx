export default function DinoDetailPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  return (
    <main className="min-h-screen">
      <div className="mx-auto max-w-[1200px] px-4 py-8">
        <p className="text-text-secondary">Dino detail page — coming soon</p>
      </div>
    </main>
  );
}
